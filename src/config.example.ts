/**
 * Fill me out and copy me to config.ts
 */

const baseUrl = "https://www.nhl.com/avalanche";
const pages = ["/", "/news", "/stats", "/schedule", "/roster", "/tickets"];
const widths = [320, 768, 1024, 1440, 1920];
const outputDir = "screenshots";

export { baseUrl, pages, widths, outputDir };
